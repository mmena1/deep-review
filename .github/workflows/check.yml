name: check

on:
  pull_request:
  push:
    branches: [main]

jobs:
  distribution:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - name: Check adapters and distribution
        shell: bash
        run: ./scripts/check.sh
